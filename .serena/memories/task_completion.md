# Task Completion Checklist

After completing a task:
1. Ensure code compiles: `task build`
2. Run tests: `task test`
3. If OpenAPI spec was changed: `task generate`
4. If dependencies changed: `task install`
